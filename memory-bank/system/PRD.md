# PRD — Product Requirements Document

> **📚 SOURCE DOCUMENTATION**
> Consolidates requirements from:
> - `memory-bank/system/USER-REQ.md`
> - user chat requirements

## Project Goal
Создать статически разворачиваемое web-приложение для прохождения духовных опросников, локального хранения результатов и передачи артефакта студент ↔ куратор с последующей обратной связью.

## Functional Scope
- Пользователь: имя, роль, смена роли в процессе работы.
- Опросники: выбор, прохождение, pause/resume, восстановление.
- Ответы: шкала 0..10, комментарии, фото.
- Dashboard: история по опросникам, даты, общая оценка по каждому опроснику (словами + баллом), график, экспорт JSON/CSV.
- Отчеты: формирование отчета по каждой отдельной оценке (attempt-level), экспорт «Скачать текст», экспорт plain text без оформления, печать из того же шаблона.
- Перенос данных пользователя: экспорт всех результатов и импорт полного набора результатов.
- Logout/Login portability: при logout выполняется экспорт пользовательского backup файла; вход может восстановить данные из backup.
- Роли и данные: результаты student и curator изолированы в разных локальных хранилищах; curator workflow не смешивается с личной student-историей.
- Logout portability (curator): при наличии кураторских данных формируется отдельный backup-файл профиля куратора.
- Аналитика самооценки: статистика одного выбранного опроса по списку вопросов.
- Аналитика самооценки: по каждому вопросу выбранного опросника показывается история изменения оценки во времени (если есть 2+ оценки).
- Admin профиль: единая зона для управления опросниками, переводами и операциями сверки/миграции.
- Формы: все новые формы проектируются i18n-first (поля, labels, validations).
- Переводы: панель переводов загружает существующие переводы и поддерживает edit/add.
- Редактор опросников: языковые поля вопросов динамически зависят от выбранных языков.
- Миграции опросников: добавленные вопросы добавляются, удаленные остаются в истории как отсутствующие.
- Pre-load reconciliation: сверка данных выполняется до импортирования результатов.
- Аналитика и графики: только шкала USER-REQ 0..10 с полными значениями, без low/medium/high категорий.
- Куратор: просмотр, статусы ревью, feedback по ответам.
- Инструменты: editor опросников, dynamic JSON loader, translation tool.
- Data layer: adapter pattern + migrations.
- Аналитика использования: Google Analytics page/form activity tracking (через env-конфигурацию).

## Non-Functional Scope
- Static hosting compatibility.
- Type-safe codebase (TypeScript strict).
- Local-first persistence.
- Mobile-friendly UI.

## Current Status (2026-02-11)
- Базовая сверка на этапе `UR-001..UR-034` зафиксирована в `memory-bank/system/AUDIT-2026-02-11-user-req.md`.
- Добавлены `UR-035...UR-043`; требуется обновленная полная сверка после реализации блока `TASK-005`.
- `UR-043` реализован в текущем dashboard UI (группировка по опроснику + общая оценка словами/баллом).
- `UR-036` реализован (роль `admin` + единый admin-hub для editor/translation/ops).
- `UR-040` реализован (маркировка удаленных вопросов в истории + миграционный v3-pass).
- `UR-041` реализован (обязательная pre-load reconciliation проверка перед import).
- `UR-044` реализован (logout export + restore from backup on login).
- `UR-037` реализован (translation panel подгружает текущие переводы выбранного языка).
- `UR-039` реализован (dynamic language fields в questionnaire editor).
- `UR-042` реализован (аналитика без low/medium/high категорий, полная шкала 0..10).
- `UR-047`, `UR-048` реализованы (build-aware migration trigger + build key persistence).
- `UR-049` реализован (fingerprint dedup для merge/import из разных источников).
- `UR-050`, `UR-051` реализованы (динамика оценок во времени + календарная навигация).
- `UR-053`, `UR-054`, `UR-055` реализованы (grouped curator workflow + import/export потоки для передачи студентам).
- `UR-038` реализован (добавлен audit покрытия переводов форм в TranslationManager + export report).
- `UR-035` усилен и формализован: добавлен i18n workflow checklist + автоматический guard-тест покрытия форм.
- `UR-056` реализован (question schema `requires_comment` + enforcement в Quiz runtime + editor support).
- Skill `spiritual-questionnaire-architect` обновлен под совместимость приложения.
- Закрыт follow-up по `UR-038`: отсутствующие translation keys для admin/editor/loader/chart/dashboard добавлены; coverage audit показывает `11/11 forms fully covered`.
- Добавлены baseline smoke tests для критичных flow: `useQuizSession`, `useResults`, `questionnaireSchema`, `resultsTransfer`.
- Закрыт LEVER backlog (`LEV-001`, `LEV-002`): формовые примитивы и role/language selectors вынесены в shared UI-компоненты.
- `UR-057` реализован: отчет по выбранному опроснику (readable view + markdown export + print template) с фото/комментариями/баллами.
- Исправлен reactive i18n flow: смена языка и reload больше не оставляют ключевые dashboard-компоненты в mixed RU/EN состоянии.
- Уточнена реализация `UR-034`/`UR-050`: динамика в self-assessment отображается на уровне конкретного вопроса внутри выбранного опросника.
- Реализована изоляция данных `student/curator` и отдельный curator-backup export при logout (`UR-058`, `UR-059`).
- Отчетный поток обновлен под `UR-060..UR-063`: отчет формируется по одной оценке, скрывает `questionId`, не показывает пустые comment/photo блоки, добавлены текстовые экспорты (`formatted` + `plain`).
- Добавлена интеграция GA (`UR-064`) с опциональной активацией через `VITE_GA_MEASUREMENT_ID`.
- Выполнен UX-рефакторинг student/curator кабинетов: defaults `JSON + replace` закреплены, техселектора убраны, настройки централизованы в отдельном `/profile`.
- Динамика для анализа вынесена в отдельную dashboard-вкладку `analytics` с usage tracking открытия вкладки.
- Схема опросников расширена до мультиязычного контракта (`metadata.languages`, локализованные `question/context_sources/self_check_prompts`), добавлена migration `v5`.
- Обновлен skill `spiritual-questionnaire-architect` под новый multilingual schema contract.
- Выполнен `TASK-035`: приложение подготовлено к PWA/static deployment (`manifest.webmanifest`, service worker, offline fallback, бренд-иконки `QWIZ`, auto `404.html` fallback).
- Выполнен `TASK-036`: иконка `QWIZ` переработана в ярком стиле (лист/лиана + график/календарь + зеленая галочка), icon-pack обновлен для PWA/install surface.
- Выполнен `TASK-037`: в установленном PWA реализованы уведомления о новых опросниках на сервере (периодическая проверка `questionnaires/index.json` + system notifications).
- Выполнен `TASK-038`: устранены белые «уголки» иконки; верхние уголки карточки очищены, внешний контур PNG иконок подтвержден как прозрачный (`alpha=0`) с Playwright-артефактами.
- Выполнен `TASK-041`: студент может держать на паузе несколько опросников; выбор карточки опросника резюмирует именно его paused-сессию с сохраненного вопроса.
- Зафиксирован follow-up `BUG-003`: static prerender runtime показывает `React #418` hydration mismatch, требуется отдельная стабилизация.

## Release Gate
1. Mobile audit для 320/375/768 — ✅ выполнен (`memory-bank/system/MOBILE-AUDIT-2026-02-11.md`, артефакты в `memory-bank/system/mobile-audit-artifacts/2026-02-11`).
2. Финальная user-req сверка `UR-045..UR-057` — ✅ выполнена (`memory-bank/system/AUDIT-2026-02-11-user-req.md`).
3. Baseline automated tests and i18n coverage guard — ✅ выполнены (`npm test`, `src/utils/formTranslationCoverage.test.ts`).

**Release Gate Status:** ✅ Closed
**Last Updated:** 2026-02-12 04:23
