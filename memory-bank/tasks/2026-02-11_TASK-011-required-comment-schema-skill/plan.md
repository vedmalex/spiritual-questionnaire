# Plan

1. Расширить доменную схему вопроса полем `requires_comment`.
2. Обновить normalize/validate/import paths для нового поля.
3. Добавить редактирование `requires_comment` в QuestionnaireEditor.
4. В QuizTaker блокировать переход без комментария при `requires_comment=true`.
5. Обновить `spiritual-questionnaire-architect/SKILL.md` под совместимость с приложением.
6. Прогнать typecheck + build.
