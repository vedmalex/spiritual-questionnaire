# Implementation

## Updated Files
- `package.json`
  - Удалены скрипты:
    - `deploy:student:surge`
    - `deploy:curator:surge`
    - `deploy:full:surge`
    - `deploy:surge`
- `README.md`
  - В секции деплоя оставлен только full-artifact путь (`dist/full/client`).
  - Из чеклиста убрана ссылка на `dist/<profile>/client` как deployment target.

