# Plan

1. Зафиксировать требования к пользовательской инструкции в `USER-REQ` и `WORKFLOW`.
2. Подготовить структуру документации в `docs/guides`.
3. Сгенерировать screenshot baseline через `playwright-cli` по профилям и типам экранов.
4. Перевести инструкцию в канонический flow-baseline формат (Flow ID + expected result + regression checkpoints).
5. Зафиксировать правило обязательной привязки UI-QA к затронутым `Flow ID`.
6. Синхронизировать memory-bank документы и backlog.
