# Plan

1. Добавить отдельное хранилище paused-сессий и расширить data adapter.
2. Перевести `useQuizSession` на модель `1 active + N paused`.
3. Обновить student route/list UI для резюма выбранного опросника.
4. Обновить backup/logout/restore и migration reconciliation.
5. Прогнать `npm test`, `npm run build`, Playwright smoke и сохранить артефакты.
