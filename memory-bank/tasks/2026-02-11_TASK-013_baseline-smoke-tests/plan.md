# Plan

1. Добавить unit/smoke tests для `questionnaireSchema` и `resultsTransfer`.
2. Добавить hook-level smoke tests для `useQuizSession` и `useResults`.
3. Настроить Vitest конфиг для стабильного React hooks execution.
4. Прогнать `npm test`, `npx tsc --noEmit`, `npm run build`.
