# Plan

1. Ввести runtime identity для опросников (`static`/`local`) без изменения persisted schema.
2. Изменить merge-логику загрузки опросников: возвращать оба источника вместо dedupe override.
3. Обновить UI списка опросников: отдельный блок локальных версий + метка `(локальный)`.
4. Перевести сессии/результаты и URL-навигацию на runtime-id для изоляции данных.
5. Добавить/обновить тесты и прогнать Playwright-сценарий по WF-008.
